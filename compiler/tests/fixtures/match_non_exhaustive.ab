module MatchNonExhaustive

// Phase 8: Non-exhaustive match — should produce errors

enum Status = Pending | Confirmed | Shipped | Cancelled

// Missing Shipped and Cancelled
fn describe(s: Status): String
  = match s {
    Pending => "waiting"
    Confirmed => "ready"
  }
