// QA test script — all assertions should pass
load "commerce.ab"

assert reachable Order.status -> @Shipped
assert reachable Order.status -> @Cancelled
assert not reachable Order.status -> @Refunded
assert not cycles Order.status
